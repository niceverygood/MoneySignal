App/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# Generated Config files
# NOTE: App/App/capacitor.config.json is tracked because packageClassList
# contains app-target plugins (AppleSignInPlugin, StoreKitPlugin) that
# `cap sync` cannot auto-generate. Re-add entries if cap sync wipes them.
App/App/config.xml
